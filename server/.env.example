# ============================================
# CloudNote Server 环境变量配置
# ============================================

# 服务器配置
# --------------------------------------------
PORT=3000                           # 服务器端口
HOST=0.0.0.0                       # 监听地址 (0.0.0.0 表示所有网络接口)
NODE_ENV=production                 # 运行环境 (development/production)

# 数据库配置
# --------------------------------------------
DATABASE_PATH=./data/cloudnote.db   # SQLite 数据库文件路径

# 存储配置
# --------------------------------------------
STORAGE_PATH=./storage              # 文件存储目录路径

# 缓存配置
# --------------------------------------------
CACHE_TTL=3600                      # 缓存过期时间（秒）
CACHE_CHECK_PERIOD=600              # 缓存清理检查周期（秒）

# 安全配置
# --------------------------------------------
JWT_SECRET=change-this-secret-key-in-production  # JWT 签名密钥（生产环境必须修改！）
SESSION_DURATION=86400              # 会话持续时间（秒，默认24小时）

# 管理员配置
# --------------------------------------------
ADMIN_USER=admin                    # 管理员用户名
ADMIN_PASSWORD=admin123             # 管理员密码（生产环境必须修改！）

# 应用配置
# --------------------------------------------
PATH_MIN_LENGTH=1                   # 笔记路径最小长度
PATH_MAX_LENGTH=20                  # 笔记路径最大长度
RATE_LIMIT_PER_MINUTE=60           # 每分钟请求限制

# ============================================
# 生产环境部署注意事项：
# 1. JWT_SECRET 必须修改为随机生成的强密钥
#    Linux/Mac: openssl rand -base64 32
#    Windows: 使用在线生成器或 PowerShell
# 2. ADMIN_PASSWORD 必须修改为强密码
# 3. 建议定期备份 data 和 storage 目录
# 4. 使用 HTTPS 进行生产部署
# ============================================